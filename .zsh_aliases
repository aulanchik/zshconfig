# General aliases

alias .='cd ..'
alias ..='cd ../../'

# Homebrew (assuming you have Homebrew installed)

alias brup='brew update'
alias brupg='brew upgrade'
alias brall='brew update && brew upgrade && brew cleanup --prune=all'
alias bic='brew install --cask '
alias bi='brew install'

# Git aliases

alias ga='git add'
alias gaa='git add --all'
alias gb='git branch'
alias gc='git commit'
alias gcm='git commit -m'
alias gco='git checkout'
alias gd='git diff'
alias gf='git fetch'
alias gl='git log'
alias gm='git merge'
alias gp='git push'
alias gr='git rebase'
alias gs='git status'


# VSCode Profiles (requires profile name)
# Requires profiles setup in vscode first
# Example: Assuming you have created Python profile called 'python' and you wish to open your current directory using that specific profile.

alias pycode='code --profile python .'

# Explanation: by executing 'pycode' in terminal, you'll launch VSCode in the current directory with the 'python' profile.
